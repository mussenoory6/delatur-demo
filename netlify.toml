# Netlify config for Vite SPA
[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: serve index.html for all routes (for future client-side routing)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
